# Kruiden AI Chat

Een eenvoudige statische webapp met een Nederlandstalige kruidenassistent en chatinterface.

## Starten

Open `index.html` direct in de browser of start lokaal een simpele webserver:

```bash
python3 -m http.server 8000
```

Bezoek daarna `http://localhost:8000`.
